import { Button } from 'antd';
import './index.less';

  return (
    <div className={'login'}>
      <Button type="primary">
        Login
      </Button>
      <div className={'text'}>Hello World</div>
    </div>
  );
}

export default Login;
